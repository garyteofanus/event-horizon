---
phase: 07-retroactive-verification-and-requirements-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - handler.go
  - handler_test.go
autonomous: true
requirements:
  - COPY-01
  - COPY-02
  - COPY-03
  - COPY-04
  - COPY-05
  - FMT-01
  - FMT-02
  - FMT-03
  - FMT-04
  - KEY-01
  - KEY-02

must_haves:
  truths:
    - "formatRequestLine function no longer exists in handler.go"
    - "TestFormatRequestLine, TestFormatRequestLineURITruncation, and TestFormatRequestLineSubMillisecond no longer exist in handler_test.go"
    - "All remaining tests pass and the project builds cleanly"
  artifacts:
    - path: "handler.go"
      provides: "HTTP handler without dead formatRequestLine code"
    - path: "handler_test.go"
      provides: "Handler tests without dead formatRequestLine test functions"
  key_links:
    - from: "handler.go"
      to: "tui.go"
      via: "renderRequestRow is the live replacement for formatRequestLine"
      pattern: "renderRequestRow"
---

<objective>
Remove the dead formatRequestLine function from handler.go and its 3 test functions from handler_test.go.

Purpose: Clean up dead code identified by the v1.1 milestone audit. formatRequestLine was created in Phase 3 and superseded by renderRequestRow in Phase 4. No production code calls it.

Output: Cleaned handler.go and handler_test.go with all existing tests passing.
</objective>

<execution_context>
@/Users/teo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/teo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-retroactive-verification-and-requirements-cleanup/07-RESEARCH.md

Source files to modify:
@handler.go
@handler_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove formatRequestLine and its tests, verify build and test suite</name>
  <files>handler.go, handler_test.go</files>
  <action>
    1. Read handler.go and locate the `formatRequestLine` function (currently at lines 82-100 per research, but verify actual line numbers). Remove the entire function including its doc comment.

    2. Read handler_test.go and locate these 3 test functions:
       - TestFormatRequestLine
       - TestFormatRequestLineURITruncation
       - TestFormatRequestLineSubMillisecond
       Remove all three functions entirely.

    3. Check if removing formatRequestLine leaves any unused imports in handler.go. The function uses `fmt.Sprintf` and `time` -- verify these are still needed by remaining code before removing any imports.

    4. Run `go build -o /dev/null .` to confirm clean build.

    5. Run `go test ./...` to confirm all remaining tests pass.

    6. Run `grep -rn "formatRequestLine" *.go` to confirm zero references remain.

    The research confirms no callers exist besides the 3 test functions. handler_test.go should go from 8 test functions to 5.
  </action>
  <verify>
    <automated>cd /Users/teo/Developer/event-horizon && go build -o /dev/null . && go test ./... && ! grep -q "formatRequestLine" handler.go handler_test.go</automated>
  </verify>
  <done>formatRequestLine and its 3 test functions are removed. Project builds cleanly, all remaining tests pass, zero references to formatRequestLine in the codebase.</done>
</task>

</tasks>

<verification>
- `go build -o /dev/null .` succeeds
- `go test ./...` passes (all remaining tests)
- `grep -rn "formatRequestLine" *.go` returns no matches
- handler_test.go has 5 test functions (down from 8)
</verification>

<success_criteria>
- formatRequestLine function removed from handler.go
- Three dead test functions removed from handler_test.go
- No unused imports left behind
- Build and full test suite pass
- No remaining references to formatRequestLine anywhere in .go files
</success_criteria>

<output>
After completion, create `.planning/phases/07-retroactive-verification-and-requirements-cleanup/07-02-SUMMARY.md`
</output>
