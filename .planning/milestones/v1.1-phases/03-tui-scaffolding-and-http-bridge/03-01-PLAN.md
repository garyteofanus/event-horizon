---
phase: 03-tui-scaffolding-and-http-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - handler.go
  - handler_test.go
  - main.go
  - main_test.go
autonomous: true
requirements: [TUI-02, TUI-03]

must_haves:
  truths:
    - "HTTP handler sends RequestData to a buffered channel on every request"
    - "Channel send is non-blocking (uses select/default to avoid slowing HTTP responses)"
    - "JSON structured logs go to file only, not stdout"
    - "Existing log fields (method, URI, status, headers, body, etc.) are preserved in file output"
  artifacts:
    - path: "handler.go"
      provides: "RequestData struct, handleRequest with channel send, formatRequestLine"
      exports: ["RequestData", "handleRequest", "formatRequestLine"]
    - path: "handler_test.go"
      provides: "Tests for channel bridge, file-only logging, request line formatting"
      contains: "TestRequestChannel"
    - path: "main.go"
      provides: "Stripped-down orchestration (file-only logger, channel creation)"
    - path: "main_test.go"
      provides: "Updated tests compatible with new handler signature"
  key_links:
    - from: "handler.go"
      to: "channel"
      via: "select with default in handleRequest"
      pattern: "select.*case reqCh.*default"
    - from: "handler.go"
      to: "slog logger"
      via: "LogAttrs call in handleRequest"
      pattern: "logger\\.LogAttrs"
---

<objective>
Split the codebase into handler.go and update main.go, introducing the RequestData struct, channel-based HTTP-to-TUI bridge, and file-only JSON logging.

Purpose: Establish the data contract (RequestData) and channel bridge that Plan 02's TUI model will consume. Switch logging from dual stdout+file to file-only so the TUI can own stdout in Plan 02.

Output: handler.go with RequestData type + adapted handler + formatRequestLine, handler_test.go with unit tests, updated main.go and main_test.go.
</objective>

<execution_context>
@/Users/teo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/teo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tui-scaffolding-and-http-bridge/03-CONTEXT.md
@.planning/phases/03-tui-scaffolding-and-http-bridge/03-RESEARCH.md

<interfaces>
<!-- Current handler signature that must change -->
From main.go:
```go
func handleRequest(logger *slog.Logger) http.HandlerFunc {
    return func(w http.ResponseWriter, r *http.Request) {
        // ... logs all request fields via logger.LogAttrs ...
    }
}
```

From main.go (current dual output -- must become file-only):
```go
writer := io.MultiWriter(os.Stdout, logFile)
logger := slog.New(slog.NewJSONHandler(writer, nil))
```

From main_test.go (test helper -- must update for new handler signature):
```go
func setupTest() (*bytes.Buffer, http.HandlerFunc) {
    var buf bytes.Buffer
    logger := slog.New(slog.NewJSONHandler(&buf, nil))
    handler := handleRequest(logger)
    return &buf, handler
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create handler.go with RequestData, channel bridge, and formatRequestLine + tests</name>
  <files>handler.go, handler_test.go</files>
  <behavior>
    - TestRequestChannel: handleRequest with a channel of size 1 receives a RequestData after serving a request; verify Method, URI, Status fields populated correctly
    - TestRequestChannelNonBlocking: handleRequest with a full (size 0 unbuffered or size 1 already-full) channel still returns HTTP 200 without hanging (the send is dropped)
    - TestFileOnlyLogging: handleRequest with a buffer-backed logger writes JSON to the buffer (proving it logs to whatever writer is given -- file-only is wired in main.go)
    - TestFormatRequestLine: formatRequestLine produces "HH:MM:SS METHOD /path STATUS TIMEms" format
    - TestFormatRequestLineURITruncation: URI longer than 40 chars is truncated to 37 chars + "..."
    - TestFormatRequestLineSubMillisecond: response time under 1ms formats as "<1ms"
  </behavior>
  <action>
    Create handler.go in package main with:

    1. RequestData struct with fields: Timestamp (time.Time), Method (string), URI (string), Status (int), ResponseTime (time.Duration).

    2. handleRequest function with signature: `func handleRequest(logger *slog.Logger, reqCh chan<- RequestData) http.HandlerFunc`. Move the existing handler body from main.go into this function. After the existing LogAttrs call, add a non-blocking channel send using select/default:
    ```go
    select {
    case reqCh <- RequestData{Timestamp: start, Method: r.Method, URI: r.RequestURI, Status: 200, ResponseTime: elapsed}:
    default:
    }
    ```

    3. formatRequestLine function: Takes RequestData, returns string in format "HH:MM:SS METHOD /path STATUS TIMEms". Use time.Format("15:04:05") for timestamp. Truncate URI at 40 chars (show first 37 + "..."). Use "<1ms" for sub-millisecond response times, otherwise fmt.Sprintf("%dms", d.Milliseconds()).

    Create handler_test.go with the tests described in behavior. Use httptest.NewRequest and httptest.NewRecorder. For channel tests, create a channel, call the handler, then read from the channel with a timeout (select with time.After(1*time.Second)).

    IMPORTANT: The handler signature changes from `handleRequest(logger)` to `handleRequest(logger, reqCh)`. This breaks existing tests in main_test.go -- that is fixed in Task 2.
  </action>
  <verify>
    <automated>cd /Users/teo/Developer/blackhole-server && go build ./... 2>&1 | head -5; go test -run "TestRequestChannel|TestFileOnlyLogging|TestFormatRequestLine" -v 2>&1 | tail -20</automated>
  </verify>
  <done>handler.go exists with RequestData struct, handleRequest accepting channel param, formatRequestLine. handler_test.go passes all 6 test cases. Code compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update main.go to file-only logging + fix main_test.go for new handler signature</name>
  <files>main.go, main_test.go</files>
  <action>
    Update main.go:
    1. Remove the handleRequest function (it now lives in handler.go).
    2. Change the logger from `io.MultiWriter(os.Stdout, logFile)` to file-only: `slog.New(slog.NewJSONHandler(logFile, nil))`.
    3. Remove the `io` import (no longer needed for MultiWriter). Keep `os` import for Getenv, OpenFile, Stderr, Exit.
    4. Create a buffered channel: `reqCh := make(chan RequestData, 256)` -- 256 per research recommendation.
    5. Update handler registration: `http.HandleFunc("/", handleRequest(logger, reqCh))`.
    6. Keep the HTTP server start as-is for now (Plan 02 will move it to a goroutine and add TUI).
    7. Remove the "server starting" log message (it would go to file only, and Plan 02 will have the TUI show the header instead). Or keep it logging to file -- either way is fine since it only goes to file now.

    Update main_test.go:
    1. Update setupTest() to create a channel and pass it: `ch := make(chan RequestData, 1)` and `handler := handleRequest(logger, ch)`. Return the channel too if any test needs it, or just discard it for tests that only care about logging.
    2. Remove TestQA01NoDeps (bubbletea will be added as a dependency in Plan 02; this test would fail).
    3. Update TestQA02LogAttrsOnly to grep both main.go and handler.go (handler now contains LogAttrs calls).
    4. Verify all existing tests still pass with the updated setupTest helper.
  </action>
  <verify>
    <automated>cd /Users/teo/Developer/blackhole-server && go test -v ./... 2>&1 | tail -30</automated>
  </verify>
  <done>main.go uses file-only logger with channel creation. All tests in main_test.go and handler_test.go pass. No compilation errors. io.MultiWriter with os.Stdout is gone.</done>
</task>

</tasks>

<verification>
All tests pass: `go test -v ./...`
Compilation clean: `go build ./...`
No stdout in logger: grep for `os.Stdout` in main.go should only appear in error handling (Stderr), not in logger setup
handler.go exports RequestData struct with all required fields
</verification>

<success_criteria>
- handleRequest accepts both logger and channel parameters
- Channel send is non-blocking (select/default pattern)
- formatRequestLine produces exact format: "HH:MM:SS METHOD /path STATUS TIMEms"
- URI truncation at 40 chars with "..." suffix works
- Sub-millisecond times display as "<1ms"
- Logger writes to file only (no os.Stdout in MultiWriter)
- All existing tests updated and passing
- All new handler_test.go tests passing
</success_criteria>

<output>
After completion, create `.planning/phases/03-tui-scaffolding-and-http-bridge/03-01-SUMMARY.md`
</output>
