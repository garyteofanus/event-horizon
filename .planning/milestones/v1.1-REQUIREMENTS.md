# Requirements Archive: v1.1 TUI Log Viewer

**Archived:** 2026-03-09
**Status:** SHIPPED

Fresh requirements for the next milestone should be created with `$gsd-new-milestone`.

## Outcome Summary

- Validated: all 23 v1.1 requirement IDs (`TUI-01`-`TUI-03`, `DISP-01`-`DISP-04`, `INTR-01`-`INTR-04`, `ROBU-01`, `COPY-01`-`COPY-05`, `FMT-01`-`FMT-04`, `KEY-01`-`KEY-02`)
- Adjusted during delivery: the milestone expanded from the original TUI/display/interaction scope to include copy and JSON formatting work in Phase 6, then closed traceability and verification gaps in Phase 7 without changing shipped behavior
- Dropped: none
- Deferred to the next milestone: `LOG-07`-`LOG-10`, `SAFE-01`-`SAFE-02`, `FILT-01`-`FILT-02`, `ROBU-02`-`ROBU-04`, `COMP-01`

---

# Requirements: event-horizon

**Defined:** 2026-03-06
**Core Value:** Every request that hits the server is reliably captured and logged in structured JSON format

## v1.0 Requirements (Complete)

All v1.0 requirements shipped and validated.

### Logging

- [x] **LOG-01**: Server logs every request as structured JSON via Go's `slog` package with `JSONHandler`
- [x] **LOG-02**: Each log entry includes timestamp, method, URI, protocol, status code, and response time
- [x] **LOG-03**: Each log entry includes all request headers as structured attributes
- [x] **LOG-04**: Each log entry includes request body content
- [x] **LOG-05**: Each log entry includes client IP (from RemoteAddr) and User-Agent as top-level fields
- [x] **LOG-06**: Each log entry includes Content-Length

### Output

- [x] **OUT-01**: JSON logs are written to stdout
- [x] **OUT-02**: JSON logs are simultaneously written to a log file
- [x] **OUT-03**: Log file path is configurable via `LOG_FILE` env var (default: `requests.log`)

### Server

- [x] **SRV-01**: Server accepts any HTTP method and any path
- [x] **SRV-02**: Server responds with 200 OK and empty body for all requests
- [x] **SRV-03**: Server listens on configurable port via `PORT` env var (default: 8080)

### Quality

- [x] **QA-01**: Zero external dependencies — stdlib only
- [x] **QA-02**: Use `slog.LogAttrs` with typed `slog.Attr` constructors (no raw key-value pairs)

## v1.1 Requirements

Requirements for the TUI Log Viewer milestone. Each maps to roadmap phases 3-7.

### TUI Core

- [x] **TUI-01**: Server launches a Bubble Tea v2 TUI on the main goroutine while the HTTP server runs in the background
- [x] **TUI-02**: HTTP handler sends request data to the TUI via a buffered channel
- [x] **TUI-03**: Structured JSON logging writes to file only while the TUI owns stdout

### Display

- [x] **DISP-01**: Each request displays as a compact one-line row with timestamp, method, path, and status code
- [x] **DISP-02**: HTTP methods are color-coded (GET=green, POST=blue, DELETE=red, PUT=yellow, PATCH=cyan)
- [x] **DISP-03**: Status codes are color-coded (2xx=green, 4xx=yellow, 5xx=red)
- [x] **DISP-04**: Visual separation exists between adjacent log entries

### Interaction

- [x] **INTR-01**: User can navigate entries with `j`/`k` or arrow keys
- [x] **INTR-02**: User can expand and collapse entries to view headers, body, client IP, and response time
- [x] **INTR-03**: User can clear visible log entries with a keybind
- [x] **INTR-04**: Footer displays available keybindings

### Robustness

- [x] **ROBU-01**: TUI adapts to terminal resize events

### Clipboard and Formatting

- [x] **COPY-01**: User can press `c` to copy the selected request body to the clipboard
- [x] **COPY-02**: User can press `Shift+C` to copy the full request to the clipboard
- [x] **COPY-03**: A success flash message appears after copy
- [x] **COPY-04**: Empty body shows a "No body to copy" flash message
- [x] **COPY-05**: Flash messages auto-dismiss after about two seconds
- [x] **FMT-01**: JSON bodies are auto-detected and displayed formatted with syntax highlighting
- [x] **FMT-02**: User can press `f` to toggle between formatted and raw body display
- [x] **FMT-03**: Body section label reflects whether the body is JSON or raw
- [x] **FMT-04**: Syntax highlighting uses distinct colors for keys, strings, numbers, booleans, and null
- [x] **KEY-01**: Clear is remapped to `x` only; `c` is reserved for copy
- [x] **KEY-02**: `Shift+C` copies full request details

## Deferred Backlog

These items were deliberately kept out of v1.1 and should be reconsidered during the next milestone definition step.

### Logging Enhancements

- **LOG-07**: Request ID generation (unique per request, also set as response header)
- **LOG-08**: Log level configuration via `LOG_LEVEL` env var
- **LOG-09**: Pretty-print text mode via `LOG_FORMAT=text` env var
- **LOG-10**: Query parameters parsed as structured key-value pairs

### Safety

- **SAFE-01**: Body size limit via `http.MaxBytesReader` (configurable, default 1 MB)
- **SAFE-02**: Sensitive header redaction (Authorization, Cookie)

### Filtering

- **FILT-01**: User can filter requests by method, path, or status code
- **FILT-02**: User can search through request history

### Robustness

- **ROBU-02**: Bounded memory with max entry cap to prevent unbounded growth
- **ROBU-03**: Graceful shutdown with context cancellation
- **ROBU-04**: Panic recovery to restore terminal on crash

### Compatibility

- **COMP-01**: `--no-tui` flag to restore v1.0 stdout JSON behavior for CI or piping

## Requirement Traceability

| Requirement | Phase | Final Status |
|-------------|-------|--------------|
| LOG-01 | Phase 1 | validated |
| LOG-02 | Phase 1 | validated |
| LOG-03 | Phase 1 | validated |
| LOG-04 | Phase 1 | validated |
| LOG-05 | Phase 1 | validated |
| LOG-06 | Phase 1 | validated |
| OUT-01 | Phase 1 | validated |
| OUT-02 | Phase 2 | validated |
| OUT-03 | Phase 2 | validated |
| SRV-01 | Phase 1 | validated |
| SRV-02 | Phase 1 | validated |
| SRV-03 | Phase 1 | validated |
| QA-01 | Phase 1 | validated |
| QA-02 | Phase 1 | validated |
| TUI-01 | Phase 3 | validated |
| TUI-02 | Phase 3 | validated |
| TUI-03 | Phase 3 | validated |
| DISP-01 | Phase 4 | validated |
| DISP-02 | Phase 4 | validated |
| DISP-03 | Phase 4 | validated |
| DISP-04 | Phase 4 | validated |
| INTR-01 | Phase 5 | validated |
| INTR-02 | Phase 5 | validated |
| INTR-03 | Phase 5 | validated |
| INTR-04 | Phase 5 | validated |
| ROBU-01 | Phase 5 | validated |
| COPY-01 | Phase 6 | validated |
| COPY-02 | Phase 6 | validated |
| COPY-03 | Phase 6 | validated |
| COPY-04 | Phase 6 | validated |
| COPY-05 | Phase 6 | validated |
| FMT-01 | Phase 6 | validated |
| FMT-02 | Phase 6 | validated |
| FMT-03 | Phase 6 | validated |
| FMT-04 | Phase 6 | validated |
| KEY-01 | Phase 6 | validated |
| KEY-02 | Phase 6 | validated |

---
*Archived at milestone completion on 2026-03-09*
